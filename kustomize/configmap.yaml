apiVersion: v1
kind: ConfigMap
metadata:
  name: python-api-demo-config
data:
  config.json: |
    {
      "environment" : "dev"
    }
# kubectl create configmap example-config --from-file ./golang/configs/config.json
